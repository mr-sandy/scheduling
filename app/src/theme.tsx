import { createTheme } from "@mui/material/styles";
import * as colours from "@mui/material/colors";

export const theme = createTheme({
  palette: {
    primary: colours.lightBlue,
    secondary: colours.deepOrange,
  },
});
